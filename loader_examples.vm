/*
 * Text file generated by $generator.name ( version $generator.version )
 * Created on $today.date ( Date ISO $today.date("yyyy-MM-dd") - Time $today.time )
 */

Instance of class "MyData"
#set($mydata = $loader.newInstance("MyData") ) 
MyData class usage :
 . ID   = $mydata.id
 . NAME = $mydata.name 

Instance of class "org.foo.FooData"
#set($foodata = $loader.newInstance("org.foo.FooData") ) 
org.foo.FooData usage :
 . number1 = $foodata.number1 
 . number2 = $foodata.number2
 . sum = $foodata.sum() 
$foodata.addNumber1(20)
addNumber1(20)...
 . number1 = $foodata.number1 

Loading of class "java.lang.Math" (without instance)
#set($math = $loader.loadClass("java.lang.Math") )
java.lang.Math usage :
 . pow(2, 8) = $math.pow(2, 8)
 . max(12, 3365) = $math.max(12, 3365)

