/*
 * Created on $today.date ( Time $today.time )
 * Generated by $generator.name ( version $generator.version )
 */

Examples of 'String' methods usage 
==================================
For all information about 'String' methods see : https://docs.oracle.com/javase/7/docs/api/java/lang/String.html 

---------------------------------------

Example 1 : 'length'
#set($STR="foo")
STR = "$STR" 
length = $STR.length() 

---------------------------------------

Example 2 : replacing a part of a string with 'replaceFirst(regex,replacement)'
#set($STR="abcdef")
STR before = "$STR"  
#set($STR = $STR.replaceFirst("def", "xy" ) )
## "abcxy"
STR after  = "$STR"  

---------------------------------------

Example 3 : checking string with 'endsWith(suffix)' and 'startsWith(prefix)'
#set($STR="abcdef")
STR = "$STR" 
#if ( $STR.endsWith("ef") ) 
YES, it ends with "ef"
#else
NO
#end
#if ( $STR.startsWith("de") ) 
YES, it starts with "de"
#else
NO, it doesn't start with "de"
#end

---------------------------------------

Example 4 : 'charAt(index)' 
#set($STR="abcdef")
STR = "$STR" 
charAt(0) : $STR.charAt(0)
charAt(2) : $STR.charAt(2)
charAt(9) : Error 'index out of range'

---------------------------------------

Example 5 : 'equalsIgnoreCase(s)' 
#set($STR="abc")
#set($STR2="AbC")
STR  = "$STR" 
STR2 = "$STR2" 
#if ( $STR.equalsIgnoreCase($STR2) ) 
YES
#else
NO
#end

---------------------------------------

Example 6 : 'toUpperCase()' and 'toLowerCase()'

#set($STR="aBcDeF")
STR  = "$STR" 
toUpperCase() : $STR.toUpperCase()
toLowerCase() : $STR.toLowerCase()

---------------------------------------
