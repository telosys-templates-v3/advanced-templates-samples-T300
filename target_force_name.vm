/*
 * Text file generated by $generator.name ( version $generator.version )
 * Created on $today.date ( Date ISO $today.date("yyyy-MM-dd") - Time $today.time )
 */

Example of target with a generated file set dynamically 

Target before 'forceEntityName' :
 
 . target.targetName             = "$target.targetName"
 . target.folder                 = "$target.folder"
 . target.template               = "$target.template"

 . target.entityName             = "$target.entityName"
 . target.forcedEntityName       = "$target.forcedEntityName"
 . target.originalFileDefinition = "$target.originalFileDefinition"
 . target.file                   = "$target.file"

Force BEANNAME
#set ( $newName = "${entity.name}Foo" )
the new name will be : ${newName}
call forceEntityName()... $target.forceEntityName(${newName})

 . target.entityName             = "$target.entityName"
 . target.forcedEntityName       = "$target.forcedEntityName"
 . target.originalFileDefinition = "$target.originalFileDefinition"
 . target.file                   = "$target.file"


