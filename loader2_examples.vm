/*
 * Text file generated by $generator.name ( version $generator.version )
 * Created on $today.date ( Date ISO $today.date("yyyy-MM-dd") - Time $today.time )
 *
 * Usage examples for the new version of ${DOLLAR}loader (Oct 2016)
 * 
 */
${DOLLAR}project.locationFullPath = $project.locationFullPath

Loader classpath folders :
 . ${DOLLAR}loader.classesFolder ( for .class files ) :
   $loader.classesFolder
 . ${DOLLAR}loader.libFolder ( for .jar files ) :
   $loader.libFolder

Loader classpath URLs ( ${DOLLAR}loader.URLs ) :
#foreach( $url in $loader.URLs )
 . $url
#end

Loading of the class "org.apache.commons.lang3.StringUtils" located in a '.jar' file in the 'lib' folder
( no instantiation because it's a static class )
#set($strUtils = $loader.loadClass("org.apache.commons.lang3.StringUtils") )
org.apache.commons.lang3.StringUtils usage :
 . difference("aaabbb", "aaccc") = $strUtils.difference("aaabbb", "aaccc")
 . lowerCase("AbCdEfG") = $strUtils.lowerCase("AbCdEfG")
 
Loading and instantiation of the class "org.foo.MyFooClass" located in the 'classes' folder
( this class uses org.apache.commons.lang3.BooleanUtils located in a '.jar' file in the 'lib' folder )
#set($myfooclass = $loader.newInstance("org.foo.MyFooClass") ) 
org.foo.MyFooClass usage :
 . negate(true)  = $myfooclass.negate(true) 
 . negate(false) = $myfooclass.negate(false) 
 . capitalize("abcd") = $myfooclass.capitalize("abcd") 

