/*
 * Text file generated by $generator.name ( version $generator.version )
 * Created on $today.date ( Date ISO $today.date("yyyy-MM-dd") - Time $today.time )
 */

Example of "generator" usage

Target :
 . target.entityName = $target.entityName
 . target.file = $target.file
 . target.folder = $target.folder

#if ( $target.entityName != "" )
Calling the generator to generate the sub file in the same folder
$generator.generate($target.entityName ,  "generator_sub_file_${entity.name}.txt", $target.folder, "generator_sub_file.vm"  )
#else
No current entity => cannot call generator without entity
#end

